name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-and-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Report edge functions count
        run: |
          echo "Counting edge functions in supabase/functions ..."
          COUNT=$(ls -1 supabase/functions | wc -l || echo 0)
          echo "Edge functions (local folders): $COUNT"
          echo "Listing:"
          ls -1 supabase/functions || true

      - name: Build app
        run: npm run build

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run E2E tests (Playwright)
        run: npx playwright test

      - name: Lighthouse CI (perf/a11y/SEO budgets)
        uses: treosh/lighthouse-ci-action@v10
        with:
          configPath: ./lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true
